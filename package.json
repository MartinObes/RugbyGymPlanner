{
  "name": "coachlab",
  "private": true,
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "dev": "sst dev",
    "deploy": "sst deploy",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "seed": "tsx scripts/seed.ts"
  },
  "devDependencies": {
    "sst": "^3",
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0"
  }
}
