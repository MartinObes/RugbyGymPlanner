# F0 — Setup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dejar el monorepo desplegando a un stage personal de AWS: tabla DynamoDB con sus dos GSI,
entidades ElectroDB, API Hono que expone OpenAPI, Nuxt SSR en Lambda detrás de CloudFront, y las
primeras funciones puras de dominio con tests en verde.

**Architecture:** pnpm workspaces con tres paquetes. `core` es puro TypeScript compartido: el dominio
no sabe que existe AWS y por eso también corre en el browser. `api` es una Lambda con Hono; `web` es
Nuxt en SSR sobre otra Lambda. SST define la infra y linkea recursos, así que la Lambda descubre el
nombre de la tabla por `Resource`, sin variables de entorno a mano.

**Tech Stack:** SST v3, AWS (CloudFront, Lambda, DynamoDB), Hono, `@hono/zod-openapi`, ElectroDB,
Nuxt 4 + Nuxt UI, Zod, Vitest, pnpm.

---

> **Nota sobre SST v3:** la superficie de API de los componentes (`sst.aws.Router`, `sst.aws.Nuxt`)
> se mueve rápido. Los snippets de infra de este plan son correctos al momento de escribirlos, pero
> **verificá contra https://sst.dev/docs/component/aws/ antes de pelear con un error de tipos**: si
> el componente cambió de forma, la corrección es alinear con la doc, no inventar un workaround.

## File Structure

| Archivo | Responsabilidad |
|---|---|
| `pnpm-workspace.yaml`, `package.json`, `tsconfig.base.json` | Monorepo |
| `sst.config.ts` | App SST y orden de carga de la infra |
| `infra/storage.ts` | Tabla DynamoDB + GSI1 + GSI2 |
| `infra/secrets.ts` | `JwtSecret` |
| `infra/api.ts` | Lambda de la API + Router de CloudFront |
| `infra/web.ts` | Nuxt SSR en Lambda |
| `packages/core/src/domain/normName.ts` | Normalización de nombres |
| `packages/core/src/domain/positions.ts` | Las 8 posiciones y los 2 grupos system |
| `packages/core/src/domain/calcLoad.ts` | `roundToHalf`, `calcLoad` |
| `packages/core/src/domain/rmFor.ts` | Match tolerante de 1RM |
| `packages/core/src/entities/index.ts` | Cliente + Service de ElectroDB |
| `packages/core/src/entities/*.ts` | Una entidad por archivo |
| `packages/api/src/index.ts` | App Hono + handler de Lambda |
| `packages/web/nuxt.config.ts` | Nuxt SSR + Nuxt UI |
| `scripts/seed.ts` | Catálogo de ejercicios + admin |

---

### Task 0: Prerrequisitos de máquina

- [ ] **Step 1: Instalar pnpm**

```powershell
corepack enable pnpm
corepack prepare pnpm@latest --activate
pnpm -v
```

Si `corepack enable` falla por permisos en Windows: `npm install -g pnpm`.
Expected: imprime `10.x` o superior.

- [ ] **Step 2: Configurar credenciales de AWS**

```powershell
aws configure
aws sts get-caller-identity
```

Expected: devuelve `Account`, `Arn` y `UserId`. Si no tenés el CLI, instalalo primero — SST necesita
credenciales resolvibles, no acepta que se las pases por parámetro.

---

### Task 1: Monorepo

**Files:**
- Create: `package.json`, `pnpm-workspace.yaml`, `tsconfig.base.json`, `.gitignore`
- Create: `packages/core/package.json`, `packages/core/tsconfig.json`

- [ ] **Step 1: `pnpm-workspace.yaml`**

```yaml
packages:
  - "packages/*"
```

- [ ] **Step 2: `package.json` raíz**

```json
{
  "name": "coachlab",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "sst dev",
    "deploy": "sst deploy",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "seed": "tsx scripts/seed.ts"
  },
  "devDependencies": {
    "sst": "^3",
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0"
  }
}
```

- [ ] **Step 3: `tsconfig.base.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "forceConsistentCasingInFileNames": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "declaration": true,
    "noEmit": true
  }
}
```

`noUncheckedIndexedAccess` es la que más va a molestar y la que más bugs evita en este proyecto: casi
todo el dominio indexa arrays y maps.

- [ ] **Step 4: `.gitignore`**

```
node_modules/
.sst/
.nuxt/
.output/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example
```

- [ ] **Step 5: `packages/core/package.json`**

```json
{
  "name": "@coachlab/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./domain/*": "./src/domain/*.ts",
    "./entities": "./src/entities/index.ts",
    "./validators/*": "./src/validators/*.ts",
    "./access": "./src/access/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "electrodb": "^3.0.0",
    "zod": "^3.23.0",
    "ulid": "^2.3.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0"
  }
}
```

Exports por subpath para que `web` pueda importar `@coachlab/core/domain/calcLoad` **sin arrastrar
`entities`** —y con eso el AWS SDK— al bundle del browser.

- [ ] **Step 6: `packages/core/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "include": ["src/**/*.ts"]
}
```

- [ ] **Step 7: Instalar y verificar**

```powershell
pnpm install
pnpm typecheck
```

Expected: sin errores.

- [ ] **Step 8: Commit**

```bash
git checkout -b feature/f0-setup
git add -A
git commit -m "chore: set up pnpm monorepo with core package"
```

---

### Task 2: Vitest y `normName`

**Files:**
- Create: `packages/core/vitest.config.ts`
- Create: `packages/core/src/domain/normName.ts`
- Test: `packages/core/src/domain/normName.test.ts`

- [ ] **Step 1: `vitest.config.ts`**

```ts
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts'],
  },
})
```

- [ ] **Step 2: Escribir el test que falla**

`packages/core/src/domain/normName.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { normName } from './normName'

describe('normName', () => {
  it('pasa a minúsculas', () => {
    expect(normName('Press Banca')).toBe('press banca')
  })

  it('saca acentos y diéresis', () => {
    expect(normName('Sentadilla Búlgara')).toBe('sentadilla bulgara')
    expect(normName('Elevación de Piernas')).toBe('elevacion de piernas')
  })

  it('colapsa espacios y recorta bordes', () => {
    expect(normName('  Peso   Muerto  ')).toBe('peso muerto')
    expect(normName('Press\tMilitar')).toBe('press militar')
  })

  it('conserva la ñ como carácter propio', () => {
    expect(normName('Año')).toBe('año')
  })

  it('devuelve string vacío para entrada vacía', () => {
    expect(normName('')).toBe('')
    expect(normName('   ')).toBe('')
  })
})
```

- [ ] **Step 3: Correr para ver que falla**

Run: `pnpm --filter @coachlab/core test`
Expected: FAIL — `Failed to resolve import "./normName"`.

- [ ] **Step 4: Implementar**

`packages/core/src/domain/normName.ts`:

```ts
/**
 * Normaliza un nombre de ejercicio para matching tolerante.
 * Minúsculas, sin acentos, espacios colapsados. La ñ se preserva.
 * Portado de `normName` del prototipo coach.html.
 */
export function normName(raw: string): string {
  return raw
    .normalize('NFD')
    .replace(/[̀-ͯ]/g, (match, offset, full: string) => {
      // U+0303 sobre n es la ñ: se reconstruye en vez de perderse.
      return match === '̃' && full[offset - 1] === 'n' ? match : ''
    })
    .normalize('NFC')
    .toLowerCase()
    .replace(/\s+/g, ' ')
    .trim()
}
```

- [ ] **Step 5: Correr para ver que pasa**

Run: `pnpm --filter @coachlab/core test`
Expected: PASS, 5 tests.

- [ ] **Step 6: Commit**

```bash
git add packages/core
git commit -m "feat(domain): add normName with vitest setup"
```

---

### Task 3: Posiciones y grupos system como constantes

`CLAUDE.md` §2 las saca de la base: son inmutables y leerlas de DynamoDB sería costo puro.

**Files:**
- Create: `packages/core/src/domain/positions.ts`
- Test: `packages/core/src/domain/positions.test.ts`

- [ ] **Step 1: Escribir los tests que fallan**

```ts
import { describe, expect, it } from 'vitest'
import {
  POSITIONS,
  SYSTEM_GROUPS,
  isPositionId,
  positionById,
  systemGroupForPosition,
} from './positions'

describe('POSITIONS', () => {
  it('tiene las 8 posiciones de rugby', () => {
    expect(POSITIONS).toHaveLength(8)
  })

  it('se reparte 4 forwards y 4 backs', () => {
    expect(POSITIONS.filter((p) => p.type === 'FORWARD')).toHaveLength(4)
    expect(POSITIONS.filter((p) => p.type === 'BACK')).toHaveLength(4)
  })

  it('usa ids slug estables', () => {
    expect(POSITIONS.map((p) => p.id)).toEqual([
      'primera-linea',
      'segunda-linea',
      'tercera-linea',
      'medio-scrum',
      'apertura',
      'centro',
      'wing',
      'fullback',
    ])
  })

  it('no repite ids', () => {
    expect(new Set(POSITIONS.map((p) => p.id)).size).toBe(8)
  })
})

describe('positionById', () => {
  it('devuelve la posición', () => {
    expect(positionById('wing')?.name).toBe('Wing')
  })

  it('devuelve undefined con un id inventado', () => {
    expect(positionById('hooker')).toBeUndefined()
  })
})

describe('isPositionId', () => {
  it('acepta un id válido', () => {
    expect(isPositionId('apertura')).toBe(true)
  })

  it('rechaza cualquier otra cosa', () => {
    expect(isPositionId('')).toBe(false)
    expect(isPositionId('APERTURA')).toBe(false)
  })
})

describe('SYSTEM_GROUPS', () => {
  it('son forwards y backs', () => {
    expect(SYSTEM_GROUPS.map((g) => g.id)).toEqual(['forwards', 'backs'])
  })

  it('cada uno contiene sus 4 posiciones', () => {
    expect(SYSTEM_GROUPS[0]!.positionIds).toHaveLength(4)
    expect(SYSTEM_GROUPS[1]!.positionIds).toHaveLength(4)
  })

  it('entre los dos cubren las 8 sin solaparse', () => {
    const all = SYSTEM_GROUPS.flatMap((g) => g.positionIds)
    expect(new Set(all).size).toBe(8)
  })
})

describe('systemGroupForPosition', () => {
  it('manda una primera línea a forwards', () => {
    expect(systemGroupForPosition('primera-linea')?.id).toBe('forwards')
  })

  it('manda un wing a backs', () => {
    expect(systemGroupForPosition('wing')?.id).toBe('backs')
  })

  it('devuelve null sin posición', () => {
    expect(systemGroupForPosition(null)).toBeNull()
  })
})
```

- [ ] **Step 2: Correr para ver que falla**

Run: `pnpm --filter @coachlab/core test`
Expected: FAIL.

- [ ] **Step 3: Implementar**

`packages/core/src/domain/positions.ts`:

```ts
export type PositionType = 'FORWARD' | 'BACK'

export type Position = {
  id: string
  name: string
  type: PositionType
  order: number
}

/** Las 8 de rugby. Fijas por decisión de CLAUDE.md §2 — no viven en DynamoDB. */
export const POSITIONS = [
  { id: 'primera-linea', name: 'Primera Línea', type: 'FORWARD', order: 1 },
  { id: 'segunda-linea', name: 'Segunda Línea', type: 'FORWARD', order: 2 },
  { id: 'tercera-linea', name: 'Tercera Línea', type: 'FORWARD', order: 3 },
  { id: 'medio-scrum', name: 'Medio Scrum', type: 'FORWARD', order: 4 },
  { id: 'apertura', name: 'Apertura', type: 'BACK', order: 5 },
  { id: 'centro', name: 'Centro', type: 'BACK', order: 6 },
  { id: 'wing', name: 'Wing', type: 'BACK', order: 7 },
  { id: 'fullback', name: 'Fullback', type: 'BACK', order: 8 },
] as const satisfies readonly Position[]

export type PositionId = (typeof POSITIONS)[number]['id']

export type SystemGroup = {
  id: 'forwards' | 'backs'
  name: string
  positionIds: readonly PositionId[]
}

export const SYSTEM_GROUPS: readonly SystemGroup[] = [
  {
    id: 'forwards',
    name: 'Forwards',
    positionIds: POSITIONS.filter((p) => p.type === 'FORWARD').map((p) => p.id),
  },
  {
    id: 'backs',
    name: 'Backs',
    positionIds: POSITIONS.filter((p) => p.type === 'BACK').map((p) => p.id),
  },
]

export function positionById(id: string): Position | undefined {
  return POSITIONS.find((p) => p.id === id)
}

export function isPositionId(id: string): id is PositionId {
  return POSITIONS.some((p) => p.id === id)
}

/** El grupo system al que pertenece una posición. Alimenta el nivel 30 de resolveProgram. */
export function systemGroupForPosition(positionId: string | null): SystemGroup | null {
  if (!positionId) return null
  return SYSTEM_GROUPS.find((g) => g.positionIds.includes(positionId as PositionId)) ?? null
}
```

- [ ] **Step 4: Correr para ver que pasa**

Run: `pnpm --filter @coachlab/core test`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/domain
git commit -m "feat(domain): add fixed positions and system groups as constants"
```

---

### Task 4: `calcLoad` — el WorkoutProcessor

Sostiene el diferencial del producto. Tests primero.

**Files:**
- Create: `packages/core/src/domain/calcLoad.ts`
- Test: `packages/core/src/domain/calcLoad.test.ts`

- [ ] **Step 1: Escribir los tests que fallan**

```ts
import { describe, expect, it } from 'vitest'
import { calcLoad, roundToHalf } from './calcLoad'

describe('roundToHalf', () => {
  it('redondea al 0.5 más cercano', () => {
    expect(roundToHalf(112.3)).toBe(112.5)
    expect(roundToHalf(112.1)).toBe(112)
    expect(roundToHalf(112.75)).toBe(113)
    expect(roundToHalf(0)).toBe(0)
  })
})

describe('calcLoad', () => {
  const ctx = { exerciseName: 'Press Banca', oneRmKg: 140 }

  it('WEIGHT devuelve los kg fijos', () => {
    expect(calcLoad({ loadType: 'WEIGHT', weight: 80 }, ctx)).toEqual({
      kind: 'weight',
      kg: 80,
      label: '80 kg',
    })
  })

  it('PERCENTAGE calcula sobre el 1RM y redondea a 0.5', () => {
    expect(calcLoad({ loadType: 'PERCENTAGE', percentage: 80 }, ctx)).toEqual({
      kind: 'percentage',
      kg: 112,
      percentage: 80,
      label: '80% → 112 kg',
    })
  })

  it('PERCENTAGE con 1RM impar cae en el medio kilo', () => {
    // 80% de 143 = 114.4 → 114.5
    const r = calcLoad({ loadType: 'PERCENTAGE', percentage: 80 }, { ...ctx, oneRmKg: 143 })
    expect(r.kind).toBe('percentage')
    if (r.kind === 'percentage') expect(r.kg).toBe(114.5)
  })

  it('PERCENTAGE sin 1RM avisa qué falta', () => {
    expect(
      calcLoad(
        { loadType: 'PERCENTAGE', percentage: 80 },
        { exerciseName: 'Press Banca', oneRmKg: null },
      ),
    ).toEqual({
      kind: 'missing-1rm',
      percentage: 80,
      exerciseName: 'Press Banca',
      label: '80% — falta tu 1RM de Press Banca',
    })
  })

  it('NONE no lleva carga', () => {
    expect(calcLoad({ loadType: 'NONE' }, ctx)).toEqual({ kind: 'none', label: 'Sin peso' })
  })

  it('WEIGHT sin weight cae a NONE en vez de romper la vista', () => {
    expect(calcLoad({ loadType: 'WEIGHT', weight: null }, ctx).kind).toBe('none')
  })

  it('formatea kg con decimal solo cuando lo tiene', () => {
    expect(calcLoad({ loadType: 'WEIGHT', weight: 82.5 }, ctx).label).toBe('82.5 kg')
  })
})
```

- [ ] **Step 2: Correr para ver que falla**

Run: `pnpm --filter @coachlab/core test` → FAIL.

- [ ] **Step 3: Implementar**

`packages/core/src/domain/calcLoad.ts`:

```ts
export type LoadType = 'WEIGHT' | 'PERCENTAGE' | 'NONE'

export type LoadSpec = {
  loadType: LoadType
  weight?: number | null
  percentage?: number | null
}

export type LoadContext = {
  exerciseName: string
  oneRmKg?: number | null
}

export type LoadResult =
  | { kind: 'weight'; kg: number; label: string }
  | { kind: 'percentage'; kg: number; percentage: number; label: string }
  | { kind: 'missing-1rm'; percentage: number; exerciseName: string; label: string }
  | { kind: 'none'; label: string }

/** Redondeo a 0.5 kg, igual que el prototipo. */
export function roundToHalf(kg: number): number {
  return Math.round(kg * 2) / 2
}

export function formatKg(kg: number): string {
  return `${Number.isInteger(kg) ? kg : kg.toFixed(1)} kg`
}

/**
 * Resuelve la carga que el jugador ve para un ejercicio del programa.
 * Pura: el 1RM ya viene resuelto en el contexto (ver rmFor).
 */
export function calcLoad(spec: LoadSpec, ctx: LoadContext): LoadResult {
  if (spec.loadType === 'WEIGHT' && spec.weight != null) {
    return { kind: 'weight', kg: spec.weight, label: formatKg(spec.weight) }
  }

  if (spec.loadType === 'PERCENTAGE' && spec.percentage != null) {
    if (ctx.oneRmKg == null) {
      return {
        kind: 'missing-1rm',
        percentage: spec.percentage,
        exerciseName: ctx.exerciseName,
        label: `${spec.percentage}% — falta tu 1RM de ${ctx.exerciseName}`,
      }
    }
    const kg = roundToHalf((spec.percentage / 100) * ctx.oneRmKg)
    return {
      kind: 'percentage',
      kg,
      percentage: spec.percentage,
      label: `${spec.percentage}% → ${formatKg(kg)}`,
    }
  }

  return { kind: 'none', label: 'Sin peso' }
}
```

- [ ] **Step 4: Correr para ver que pasa**

Run: `pnpm --filter @coachlab/core test` → PASS.

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/domain
git commit -m "feat(domain): add calcLoad with 0.5kg rounding for the three load types"
```

---

### Task 5: `rmFor` — matching tolerante de 1RM

**Files:**
- Create: `packages/core/src/domain/rmFor.ts`
- Test: `packages/core/src/domain/rmFor.test.ts`

- [ ] **Step 1: Escribir los tests que fallan**

```ts
import { describe, expect, it } from 'vitest'
import { rmFor } from './rmFor'

const oneRms = [
  { normalizedName: 'press banca', kg: 140 },
  { normalizedName: 'sentadilla', kg: 180 },
  { normalizedName: 'sentadilla frontal', kg: 120 },
]

describe('rmFor', () => {
  it('matchea exacto', () => {
    expect(rmFor(oneRms, 'Press Banca')).toBe(140)
  })

  it('matchea ignorando acentos y mayúsculas', () => {
    expect(rmFor([{ normalizedName: 'sentadilla bulgara', kg: 90 }], 'Sentadilla Búlgara')).toBe(90)
  })

  it('matchea por inclusión cuando el programa es más específico', () => {
    expect(rmFor(oneRms, 'Press Banca con Mancuernas')).toBe(140)
  })

  it('matchea por inclusión cuando el 1RM es más específico', () => {
    expect(rmFor([{ normalizedName: 'press banca plano', kg: 130 }], 'Press Banca')).toBe(130)
  })

  it('ante varias inclusiones gana la coincidencia más larga', () => {
    expect(rmFor(oneRms, 'Sentadilla Frontal')).toBe(120)
  })

  it('devuelve null si no hay ninguna coincidencia', () => {
    expect(rmFor(oneRms, 'Remo con Barra')).toBeNull()
  })

  it('devuelve null con lista vacía', () => {
    expect(rmFor([], 'Press Banca')).toBeNull()
  })
})
```

- [ ] **Step 2: Correr para ver que falla** → FAIL.

- [ ] **Step 3: Implementar**

`packages/core/src/domain/rmFor.ts`:

```ts
import { normName } from './normName'

export type OneRmRecord = {
  normalizedName: string
  kg: number
}

/**
 * Busca el 1RM del jugador para un ejercicio.
 * Exacto primero; si no, inclusión en cualquier dirección, ganando la coincidencia más larga.
 * Portado de `rmFor` del prototipo coach.html.
 */
export function rmFor(oneRms: OneRmRecord[], exerciseName: string): number | null {
  const target = normName(exerciseName)
  if (!target) return null

  const exact = oneRms.find((r) => r.normalizedName === target)
  if (exact) return exact.kg

  let best: OneRmRecord | null = null
  for (const record of oneRms) {
    const matches = record.normalizedName.includes(target) || target.includes(record.normalizedName)
    if (!matches) continue
    if (best === null || record.normalizedName.length > best.normalizedName.length) {
      best = record
    }
  }

  return best?.kg ?? null
}
```

- [ ] **Step 4: Correr para ver que pasa** → PASS.

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/domain
git commit -m "feat(domain): add rmFor tolerant 1RM lookup"
```

---

### Task 6: SST y la tabla

**Files:**
- Create: `sst.config.ts`, `infra/storage.ts`, `infra/secrets.ts`

- [ ] **Step 1: Inicializar SST**

```powershell
pnpm dlx sst@latest init
```

Elegir "aws" como home. Genera `sst.config.ts` y `.sst/`.

- [ ] **Step 2: Reescribir `sst.config.ts`**

```ts
/// <reference path="./.sst/platform/config.d.ts" />

export default $config({
  app(input) {
    return {
      name: 'coachlab',
      // Un stage personal se borra entero sin drama; production nunca.
      removal: input?.stage === 'production' ? 'retain' : 'remove',
      protect: input?.stage === 'production',
      home: 'aws',
    }
  },
  async run() {
    const storage = await import('./infra/storage')
    const api = await import('./infra/api')
    const web = await import('./infra/web')

    return {
      table: storage.table.name,
      api: api.api.url,
      web: web.web.url,
    }
  },
})
```

- [ ] **Step 3: `infra/storage.ts`**

```ts
/**
 * Tabla única. Dos GSI (CLAUDE.md §3):
 *  - gsi1 "listar por padre": jugadores de un coach, programas de un coach,
 *    grupos custom de un coach, catálogo de ejercicios.
 *  - gsi2 "assignments por destino": lo que alimenta resolveProgram.
 * Los nombres de índice son genéricos a propósito: varias entidades los comparten
 * con composites distintos, y la tabla no debería cambiar cuando se suma una.
 */
export const table = new sst.aws.Dynamo('CoachLab', {
  fields: {
    pk: 'string',
    sk: 'string',
    gsi1pk: 'string',
    gsi1sk: 'string',
    gsi2pk: 'string',
    gsi2sk: 'string',
  },
  primaryIndex: { hashKey: 'pk', rangeKey: 'sk' },
  globalIndexes: {
    gsi1: { hashKey: 'gsi1pk', rangeKey: 'gsi1sk' },
    gsi2: { hashKey: 'gsi2pk', rangeKey: 'gsi2sk' },
  },
})
```

- [ ] **Step 4: `infra/secrets.ts`**

```ts
export const jwtSecret = new sst.Secret('JwtSecret')
```

- [ ] **Step 5: Setear el secreto**

```powershell
pnpm dlx sst secret set JwtSecret "$([guid]::NewGuid().ToString() + [guid]::NewGuid().ToString())"
```

- [ ] **Step 6: Commit**

```bash
git add sst.config.ts infra/
git commit -m "feat(infra): add sst app with single-table dynamodb"
```

---

### Task 7: Entidades ElectroDB

**Files:**
- Create: `packages/core/src/entities/client.ts`
- Create: `packages/core/src/entities/user.ts`, `exercise.ts`, `unique.ts`
- Create: `packages/core/src/entities/index.ts`
- Test: `packages/core/src/entities/user.test.ts` (smoke de claves, sin AWS)

- [ ] **Step 1: Cliente**

`packages/core/src/entities/client.ts`:

```ts
import { DynamoDBClient } from '@aws-sdk/client-dynamodb'
import { DynamoDBDocumentClient } from '@aws-sdk/lib-dynamodb'
import { Resource } from 'sst'

const client = DynamoDBDocumentClient.from(new DynamoDBClient({}), {
  marshallOptions: { removeUndefinedValues: true },
})

/** Config compartida por todas las entidades: mismo cliente, misma tabla. */
export const entityConfig = {
  client,
  table: Resource.CoachLab.name,
}
```

- [ ] **Step 2: Entidad User**

`packages/core/src/entities/user.ts`:

```ts
import { Entity } from 'electrodb'
import { entityConfig } from './client'

/**
 * Un solo item por persona, con los atributos del rol encima.
 * No se parte en perfiles: eso es reflejo relacional y cuesta una lectura extra.
 */
export const UserEntity = new Entity(
  {
    model: { entity: 'user', version: '1', service: 'coachlab' },
    attributes: {
      userId: { type: 'string', required: true },
      email: { type: 'string', required: true },
      passwordHash: { type: 'string', required: true },
      name: { type: 'string', required: true },
      role: { type: ['PLAYER', 'COACH', 'ADMIN'] as const, required: true },

      // COACH
      inviteCode: { type: 'string' },

      // PLAYER
      coachId: { type: 'string' },
      positionId: { type: 'string' },
      heightCm: { type: 'number' },
      weightKg: { type: 'number' },

      createdAt: {
        type: 'string',
        required: true,
        readOnly: true,
        default: () => new Date().toISOString(),
      },
      updatedAt: {
        type: 'string',
        required: true,
        default: () => new Date().toISOString(),
        set: () => new Date().toISOString(),
      },
    },
    indexes: {
      byId: {
        pk: { field: 'pk', composite: ['userId'] },
        sk: { field: 'sk', composite: [] },
      },
      // Índice sparse: un COACH no tiene coachId, así que no entra en gsi1.
      byCoach: {
        index: 'gsi1',
        pk: { field: 'gsi1pk', composite: ['coachId'] },
        sk: { field: 'gsi1sk', composite: ['userId'] },
      },
    },
  },
  entityConfig,
)
```

- [ ] **Step 3: Entidades de unicidad**

`packages/core/src/entities/unique.ts`:

```ts
import { Entity } from 'electrodb'
import { entityConfig } from './client'

/**
 * Items dedicados para unicidad fuerte. Se escriben en la MISMA transacción que
 * el User, con `attribute_not_exists`. Un GSI no sirve acá: es eventualmente
 * consistente y dejaría pasar dos registros con el mismo email.
 */
export const UniqueEmailEntity = new Entity(
  {
    model: { entity: 'uniqueEmail', version: '1', service: 'coachlab' },
    attributes: {
      email: { type: 'string', required: true },
      userId: { type: 'string', required: true },
    },
    indexes: {
      byEmail: {
        pk: { field: 'pk', composite: ['email'] },
        sk: { field: 'sk', composite: [] },
      },
    },
  },
  entityConfig,
)

export const UniqueInviteCodeEntity = new Entity(
  {
    model: { entity: 'uniqueInviteCode', version: '1', service: 'coachlab' },
    attributes: {
      inviteCode: { type: 'string', required: true },
      coachId: { type: 'string', required: true },
    },
    indexes: {
      byCode: {
        pk: { field: 'pk', composite: ['inviteCode'] },
        sk: { field: 'sk', composite: [] },
      },
    },
  },
  entityConfig,
)
```

- [ ] **Step 4: Entidad Exercise**

`packages/core/src/entities/exercise.ts`:

```ts
import { Entity } from 'electrodb'
import { entityConfig } from './client'

export const ExerciseEntity = new Entity(
  {
    model: { entity: 'exercise', version: '1', service: 'coachlab' },
    attributes: {
      exerciseId: { type: 'string', required: true },
      name: { type: 'string', required: true },
      normalizedName: { type: 'string', required: true },
      category: { type: 'string' },
      // Constante: el catálogo es global y chico (~48 items). Una sola partición
      // en gsi1 alcanza y sobra para la escala de CLAUDE.md §2 (~300 usuarios).
      catalog: { type: 'string', required: true, default: 'catalog', readOnly: true },
      createdAt: {
        type: 'string',
        required: true,
        readOnly: true,
        default: () => new Date().toISOString(),
      },
    },
    indexes: {
      byId: {
        pk: { field: 'pk', composite: ['exerciseId'] },
        sk: { field: 'sk', composite: [] },
      },
      byCatalog: {
        index: 'gsi1',
        pk: { field: 'gsi1pk', composite: ['catalog'] },
        sk: { field: 'gsi1sk', composite: ['normalizedName'] },
      },
    },
  },
  entityConfig,
)
```

- [ ] **Step 5: Service**

`packages/core/src/entities/index.ts`:

```ts
import { Service } from 'electrodb'
import { entityConfig } from './client'
import { ExerciseEntity } from './exercise'
import { UniqueEmailEntity, UniqueInviteCodeEntity } from './unique'
import { UserEntity } from './user'

export const db = new Service(
  {
    user: UserEntity,
    uniqueEmail: UniqueEmailEntity,
    uniqueInviteCode: UniqueInviteCodeEntity,
    exercise: ExerciseEntity,
  },
  entityConfig,
)

export { ExerciseEntity, UniqueEmailEntity, UniqueInviteCodeEntity, UserEntity }
```

> Las entidades de F2/F3 (`Program`, `Week`, `PositionGroup`, `ProgramAssignment`, `OneRM`,
> `SessionLog`, `Evaluation`) se agregan en sus fases. El Service se amplía ahí.

- [ ] **Step 6: Test de claves sin AWS**

ElectroDB puede generar las claves sin conectarse. Este test es barato y atrapa el error más caro:
que un índice sparse termine escribiendo la partición equivocada.

`packages/core/src/entities/user.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { UserEntity } from './user'

const player = {
  userId: '01J000000000000000000PLAYER',
  email: 'jugador@club.uy',
  passwordHash: 'x',
  name: 'Jugador',
  role: 'PLAYER' as const,
  coachId: '01J0000000000000000000COACH',
}

describe('UserEntity', () => {
  it('escribe el item bajo su propio userId', () => {
    const { pk } = UserEntity.put(player).params().Item
    expect(pk).toContain(player.userId.toLowerCase())
  })

  it('un PLAYER entra en gsi1 bajo su coach', () => {
    const item = UserEntity.put(player).params().Item
    expect(item.gsi1pk).toContain(player.coachId.toLowerCase())
  })

  it('un COACH no entra en gsi1', () => {
    const coach = { ...player, role: 'COACH' as const, coachId: undefined }
    const item = UserEntity.put(coach).params().Item
    expect(item.gsi1pk).toBeUndefined()
  })
})
```

> Si el tercer test falla, ElectroDB **no** está tratando `byCoach` como sparse. La corrección no es
> borrar el test: es marcar el índice como sparse según la doc de ElectroDB vigente, o separar el
> jugador en su propia entidad. Reportarlo antes de seguir.

- [ ] **Step 7: Correr**

Run: `pnpm --filter @coachlab/core test`
Expected: PASS.

- [ ] **Step 8: Commit**

```bash
git add packages/core/src/entities
git commit -m "feat(db): add electrodb entities for user, uniqueness and exercises"
```

---

### Task 8: API Hono con OpenAPI

**Files:**
- Create: `packages/api/package.json`, `tsconfig.json`
- Create: `packages/api/src/index.ts`
- Create: `infra/api.ts`

- [ ] **Step 1: `packages/api/package.json`**

```json
{
  "name": "@coachlab/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@coachlab/core": "workspace:*",
    "hono": "^4.6.0",
    "@hono/zod-openapi": "^0.18.0",
    "@node-rs/argon2": "^2.0.0",
    "zod": "^3.23.0"
  }
}
```

- [ ] **Step 2: `packages/api/src/index.ts`**

```ts
import { OpenAPIHono, createRoute, z } from '@hono/zod-openapi'
import { handle } from 'hono/aws-lambda'

export const app = new OpenAPIHono()

const healthRoute = createRoute({
  method: 'get',
  path: '/health',
  responses: {
    200: {
      description: 'La API está viva',
      content: {
        'application/json': {
          schema: z.object({ ok: z.literal(true), service: z.string() }),
        },
      },
    },
  },
})

app.openapi(healthRoute, (c) => c.json({ ok: true as const, service: 'coachlab-api' }))

// El spec que consume hey-api para generar el cliente del frontend.
app.doc('/openapi.json', {
  openapi: '3.0.0',
  info: { version: '0.1.0', title: 'CoachLab API' },
})

export const handler = handle(app)
```

- [ ] **Step 3: `infra/api.ts`**

```ts
import { jwtSecret } from './secrets'
import { table } from './storage'

export const api = new sst.aws.Function('Api', {
  handler: 'packages/api/src/index.handler',
  runtime: 'nodejs22.x',
  architecture: 'arm64',
  // link expone Resource.CoachLab / Resource.JwtSecret dentro de la Lambda
  // y le da los permisos IAM justos, sin política escrita a mano.
  link: [table, jwtSecret],
  url: true,
  // argon2 trae binario nativo: no se puede bundlear, se instala en el paquete.
  nodejs: { install: ['@node-rs/argon2'] },
})
```

> `architecture: 'arm64'` no es cosmética: `@node-rs/argon2` publica binarios para arm64 de Lambda y
> sale más barato. Si más adelante algo obliga a x64, hay que revalidar que el binario cargue.

- [ ] **Step 4: Desplegar**

```powershell
pnpm dlx sst deploy --stage <tu-nombre>
```

Expected: crea la tabla y la función, e imprime la URL de la API.

- [ ] **Step 5: Verificar**

```powershell
curl "<api-url>/health"
curl "<api-url>/openapi.json"
```

Expected: `{"ok":true,"service":"coachlab-api"}` y un documento OpenAPI con el path `/health`.

- [ ] **Step 6: Commit**

```bash
git add packages/api infra/api.ts
git commit -m "feat(api): add hono lambda with openapi spec endpoint"
```

---

### Task 9: Nuxt SSR y el cliente generado

**Files:**
- Create: `packages/web/` (scaffold de Nuxt)
- Create: `infra/web.ts`
- Create: `packages/web/openapi-ts.config.ts`

- [ ] **Step 1: Scaffold**

```powershell
cd packages
pnpm create nuxt@latest web --packageManager pnpm --no-install --gitInit false
cd ..
pnpm install
```

- [ ] **Step 2: Nuxt UI y hey-api**

```powershell
pnpm --filter @coachlab/web add @nuxt/ui
pnpm --filter @coachlab/web add -D @hey-api/openapi-ts
pnpm --filter @coachlab/web add @coachlab/core
```

- [ ] **Step 3: `packages/web/nuxt.config.ts`**

```ts
export default defineNuxtConfig({
  compatibilityDate: '2025-01-01',
  // SSR sobre Lambda: la cookie de sesión se lee en el server y el render
  // ya sale con el rol correcto (CLAUDE.md §2).
  ssr: true,
  modules: ['@nuxt/ui'],
  runtimeConfig: {
    public: {
      apiUrl: '',
    },
  },
  nitro: {
    preset: 'aws-lambda',
  },
})
```

- [ ] **Step 4: `packages/web/openapi-ts.config.ts`**

```ts
import { defineConfig } from '@hey-api/openapi-ts'

export default defineConfig({
  input: process.env.API_URL ? `${process.env.API_URL}/openapi.json` : './openapi.json',
  output: { path: './generated', format: 'prettier' },
  plugins: ['@hey-api/client-fetch'],
})
```

Y en `packages/web/package.json`, script: `"generate:api": "openapi-ts"`.

- [ ] **Step 5: Generar el cliente**

```powershell
$env:API_URL="<api-url>"; pnpm --filter @coachlab/web generate:api
```

Expected: aparecen archivos en `packages/web/generated/` con una función tipada para `/health`.

- [ ] **Step 6: `infra/web.ts`**

```ts
import { api } from './api'

export const web = new sst.aws.Nuxt('Web', {
  path: 'packages/web',
  link: [api],
  environment: {
    NUXT_PUBLIC_API_URL: api.url,
  },
})
```

- [ ] **Step 7: Página de humo**

`packages/web/app/pages/index.vue` que llame al `/health` generado y muestre el resultado. Sirve
para confirmar de punta a punta que CloudFront → Nuxt → API funciona.

- [ ] **Step 8: Desplegar y verificar**

```powershell
pnpm dlx sst deploy --stage <tu-nombre>
```

Abrir la URL de `web`. Expected: la página muestra `coachlab-api` leído de la API real.

- [ ] **Step 9: Commit**

```bash
git add packages/web infra/web.ts
git commit -m "feat(web): add nuxt ssr on lambda with generated api client"
```

---

### Task 10: Seed

⚠ **NECESITA PROTOTIPO** para el catálogo: `CLAUDE.md` §3 habla de "~48 ejercicios del WorkoutPlanner",
que salen de `NEXTJS_APP_CONTEXT.md` — ausente del repo. Abajo va un catálogo base de 24 que cubre los
patrones de fuerza usados en rugby. Cuando aparezca el archivo, **reemplazar la lista completa**: el
resto del seed no cambia porque es idempotente por `normalizedName`.

**Files:**
- Create: `scripts/seed.ts`

- [ ] **Step 1: Escribir `scripts/seed.ts`**

```ts
import { hash } from '@node-rs/argon2'
import { ulid } from 'ulid'
import { normName } from '@coachlab/core/domain/normName'
import { ExerciseEntity, UniqueEmailEntity, UserEntity } from '@coachlab/core/entities'

const EXERCISES = [
  { name: 'Sentadilla', category: 'Tren inferior' },
  { name: 'Sentadilla Frontal', category: 'Tren inferior' },
  { name: 'Sentadilla Búlgara', category: 'Tren inferior' },
  { name: 'Peso Muerto', category: 'Tren inferior' },
  { name: 'Peso Muerto Rumano', category: 'Tren inferior' },
  { name: 'Hip Thrust', category: 'Tren inferior' },
  { name: 'Prensa', category: 'Tren inferior' },
  { name: 'Zancadas', category: 'Tren inferior' },
  { name: 'Curl Femoral', category: 'Tren inferior' },
  { name: 'Extensión de Cuádriceps', category: 'Tren inferior' },
  { name: 'Press Banca', category: 'Tren superior' },
  { name: 'Press Banca Inclinado', category: 'Tren superior' },
  { name: 'Press Militar', category: 'Tren superior' },
  { name: 'Remo con Barra', category: 'Tren superior' },
  { name: 'Remo con Mancuerna', category: 'Tren superior' },
  { name: 'Dominadas', category: 'Tren superior' },
  { name: 'Fondos', category: 'Tren superior' },
  { name: 'Curl de Bíceps', category: 'Tren superior' },
  { name: 'Extensión de Tríceps', category: 'Tren superior' },
  { name: 'Cargada de Potencia', category: 'Potencia' },
  { name: 'Arranque', category: 'Potencia' },
  { name: 'Salto al Cajón', category: 'Potencia' },
  { name: 'Plancha', category: 'Core' },
  { name: 'Rueda Abdominal', category: 'Core' },
]

async function main() {
  const stage = process.env.SST_STAGE
  if (stage === 'production') {
    throw new Error('Seed bloqueado: no se corre contra production.')
  }

  for (const exercise of EXERCISES) {
    const normalizedName = normName(exercise.name)
    // Idempotente por normalizedName: el id se deriva de él, así que reejecutar pisa la misma fila.
    await ExerciseEntity.put({
      exerciseId: normalizedName,
      name: exercise.name,
      normalizedName,
      category: exercise.category,
    }).go()
  }
  console.log(`✓ ${EXERCISES.length} ejercicios`)

  const email = process.env.SEED_ADMIN_EMAIL
  const password = process.env.SEED_ADMIN_PASSWORD
  if (!email || !password) {
    throw new Error('Faltan SEED_ADMIN_EMAIL y SEED_ADMIN_PASSWORD')
  }

  const existing = await UniqueEmailEntity.get({ email }).go()
  if (existing.data) {
    console.log(`✓ admin ${email} ya existía`)
    return
  }

  const userId = ulid()
  await UserEntity.put({
    userId,
    email,
    name: 'Admin',
    role: 'ADMIN',
    passwordHash: await hash(password),
  }).go()
  await UniqueEmailEntity.put({ email, userId }).go()
  console.log(`✓ admin ${email}`)
}

main().catch((error) => {
  console.error(error)
  process.exit(1)
})
```

> El seed usa dos `put` sueltos para el admin, no la transacción de F1. Es aceptable acá porque corre
> una sola vez y de forma controlada; **la registración real de F1 sí usa `TransactWrite`**, y esa es
> la que tiene que ser correcta bajo concurrencia.

- [ ] **Step 2: Correr**

```powershell
$env:SEED_ADMIN_EMAIL="admin@coachlab.local"
$env:SEED_ADMIN_PASSWORD="cambiar-esto"
pnpm dlx sst shell --stage <tu-nombre> -- pnpm seed
```

`sst shell` inyecta los links, así que `Resource.CoachLab` resuelve.
Expected: `✓ 24 ejercicios` y `✓ admin admin@coachlab.local`.

- [ ] **Step 3: Verificar idempotencia**

Correr de nuevo. Expected: misma salida, con `ya existía` en el admin.

- [ ] **Step 4: Verificar el guard de producción**

```powershell
$env:SST_STAGE="production"; pnpm seed; Remove-Item Env:SST_STAGE
```

Expected: falla con `Seed bloqueado`.

- [ ] **Step 5: Commit**

```bash
git add scripts/seed.ts
git commit -m "feat(db): add idempotent seed for exercise catalog and admin"
```

---

### Task 11: Cierre de fase

- [ ] **Step 1: Limpiar los `.gitkeep` de carpetas que ya tienen archivos**

```bash
find infra packages -name .gitkeep -execdir sh -c '[ "$(ls -A . | grep -v .gitkeep)" ] && rm -f .gitkeep' \;
```

- [ ] **Step 2: Verificación completa**

Run: `pnpm typecheck && pnpm test`
Expected: verde. Anotar el conteo de tests (deberían ser ~35).

- [ ] **Step 3: Marcar la fase en CLAUDE.md**

En §6:

```markdown
- [x] **F0 — Setup**: monorepo pnpm + SST, tabla DynamoDB, entidades ElectroDB, Hono con OpenAPI, Nuxt SSR, funciones puras de dominio con tests, deploy al stage personal.
```

- [ ] **Step 4: Commit y merge**

```bash
git add CLAUDE.md
git commit -m "docs: mark F0 complete"
git checkout main
git merge --no-ff feature/f0-setup -m "feat: F0 setup — sst, dynamodb, hono api, nuxt ssr"
```

---

## Definición de terminado

- `sst deploy --stage <tu-nombre>` completa y devuelve URLs de API y web.
- `GET /health` responde y `GET /openapi.json` devuelve el spec.
- La página de Nuxt muestra un dato leído de la API real a través de CloudFront.
- El seed cargó el catálogo y el admin, y es idempotente.
- `pnpm typecheck && pnpm test` en verde.
