node_modules/
.nuxt/
.output/
.vercel/
.data/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example

# Artefactos del CLI de Supabase (cache de versión, credenciales de link).
supabase/.temp/
supabase/.branches/

# Generado por `pnpm dump:openapi`. Es un artefacto intermedio: la fuente es el
# schema Zod de las rutas. El cliente en packages/web/generated/ SÍ se versiona,
# para que el repo compile sin tener que regenerarlo.
packages/web/openapi.json

# Planillas reales del club. Tienen DATOS PERSONALES (la hoja "Grupos" lista
# apellidos y apodos del plantel), así que no van al repo ni siquiera en una rama.
# Para tests, los fixtures van anonimizados en packages/core/src/domain/__fixtures__/.
*.xlsx
*.xls
